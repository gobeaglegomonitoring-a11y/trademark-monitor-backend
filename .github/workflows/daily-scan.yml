name: Trademark Scan

on:
  schedule:
    # Offset 7 minutes past the hour -- GitHub's own docs call out :00 as the
    # most congested minute for scheduled workflows (everyone else schedules
    # there too), which was causing 1-2 hour queue delays.
    - cron: '7 3,9,15 * * *'  # three times daily: ~11 PM, 5 AM, and 11 AM EDT (UTC-4)
  workflow_dispatch:        # allow manual trigger from GitHub Actions tab

jobs:
  run-scan:
    name: Run All Scrapers
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Trigger scan on Render backend
        run: |
          echo "Starting daily scan at $(date -u)"
          response=$(curl -s -o /tmp/response.json -w "%{http_code}" \
            -X POST \
            -H "Content-Type: application/json" \
            -H "x-scan-api-key: ${{ secrets.SCAN_API_KEY }}" \
            --max-time 60 \
            https://trademark-monitor-backend.onrender.com/api/scan/runall)

          echo "HTTP status: $response"
          echo "Response body:"
          cat /tmp/response.json

          if [ "$response" != "200" ] && [ "$response" != "202" ]; then
            echo "Scan failed with HTTP $response"
            exit 1
          fi

          echo "Scan triggered successfully -- it now runs on Render independently of this workflow."
